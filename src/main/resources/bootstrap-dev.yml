spring:
  cloud:
    nacos:
      config:
        server-addr:
        namespace:
        group: DEFAULT_GROUP # 使用的 Nacos 配置分组，默认为 DEFAULT_GROU
        name: blue-bird-realtime
        file-extension: yaml

#logging:
#  level:
#    org:
#      springframework:
#        kafka: ERROR # spring-kafka INFO 日志太多了，所以我们限制只打印 ERROR 级别
#      apache:
#        kafka: ERROR # kafka INFO 日志太多了，所以我们限制只打印 ERROR 级别